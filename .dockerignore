# Build context 排除 — 保持 image 細 + 唔漏 secret
**/node_modules
**/.next
**/dist
**/.turbo
.turbo
.git
.gitignore
.backups
.dev-logs
.public-state
.agents
agent
design-samples
docs
*.md
# ...but the admin console serves this one at /api/changelog, so it has to be
# in the context. Re-included AFTER *.md — order matters in .dockerignore.
!CHANGELOG.md
!apps/api/prisma/**

# 永遠唔好將真 secret 入 image layer（runtime 由 compose env_file 注入）
**/.env
**/.env.*
!**/.env.*.example
