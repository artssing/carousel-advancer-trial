# QA — i18n — 2026-08-10 **(b) — post-fix run**

Run id: `qa-20260811-b`　·　實際執行日期 **2026-08-11**

> **檔名點解係 `-b`**：`docs/qa/reports/2026-08-10-i18n.md` 已經有一份（同一個 08-10
> report series，`f92e7de`，嗰次報咗 IN-11 mismatch）。今次係 **IN-11 修好之後嘅
> post-fix run**（HEAD 已經行到 `eb19c41`）。**冇覆蓋、冇改動舊嗰份** —— 揀咗
> `-b` 後綴獨立成檔，兩份可以並排對照。舊嗰份仍然係 `f92e7de` 嘅記錄。

**Deployment verified**：UAT 呢部機唔係 Docker stack，係 founder 行緊嘅 local dev
server 直接食呢個 git working tree。所以「container 行緊舊 code」結構上唔存在，但仍然逐項驗過：

1. `git rev-parse HEAD` = **`eb19c41d56fdf31b2b9c2110d2f72d4ef6a9cfc9`**（正正係 scope 個 `last_synced_commit`）。
2. `git status --porcelain` = 淨係 ` M docs/qa/scope/frontend/i18n.md`（今次 sync 嘅產物）。
   **冇任何 product code 未 commit** —— 即係唔存在「測緊一棵唔知咩狀態嘅 tree」。
3. **改動真係喺 running code 入面**：
   - source：`grep -o "ui\.conversation\.status\.[A-Z_]*" packages/ui/src/components/conversation-pane.tsx | sort -u` → **19 個**，含 `eb19c41` 新加嘅 `AWAITING_BUYER_PICKUP` / `CUSTODY` / `HANDOVER_TO_AUTH` / `SELLER_ACK_PENDING` / `PAYMENT_EXPIRED` / `AUTH_RECEIVED_PENDING_SELLER_ACK` / `DELIVERED_PENDING_AUTH_ACK`。
   - **dist（`@authentik/utils` 係 dist-consumed，呢個先係 server 真食嗰份）**：
     `grep -o "ui.conversation.status.AWAITING_BUYER_PICKUP" packages/utils/dist/locales/data.js` → 有；
     `packages/utils/dist/locales/data.js` mtime **Aug 11 01:10**（`eb19c41` 之後 rebuild 過）。
     即係 CLAUDE.md 「改完 utils 必 rebuild」呢步做咗，唔係 stale dist。
   - server 真身：`lsof -d cwd` 證實 PID 27556/27637/27638 個 cwd 就係
     `…/carousel-advancer-trial/apps/{api,consumer,authenticator}`（同一棵 tree）。
4. 四個 port 應機：`3018` / `3011` / `3013` → 200；`4010` `/api/listings` → 200。
   （`4010` 個 root `/` 回 404 係正常 —— NestJS global prefix `/api`。）

**Substitution 記錄**：scope 入面所有 `https://uat.certifinehk.com` /
`uat-auth.certifinehk.com` / `uat-admin.certifinehk.com` / `uat-api…`，一律改打
`http://localhost:3018` / `:3011` / `:3013` / `:4010`。**冇掂 PROD**。
founder 三個 dev server process（27556 / 27637 / 27638）**由頭到尾冇 kill 冇 restart**。

Lane budget: curl 3 · static 11 · browser 4 · manual 2（唔跑）· unverified 7（唔計數）
Cases: **11 run · 11 matched · 0 mismatched · 0 skipped**
Known, unchanged: **0**（`known.md` 五條 WA-06 / CC-01 / CC-06 / CB-03 / AP-02 全部唔屬呢個 scope）

> 計數口徑（README「Status」）：20 條入面，**7 條標 `unverified`** 唔計入總數，
> **2 條 `manual`** 唔屬我。計數嘅 11 條 = static 10 + curl 1，全部行到、全部 match。
> 另外我**額外行咗** 3 條 unverified（IN-21 / IN-15 / IN-16），結果照記低但**唔計入 11 條**。

---

## 今次 run 嘅結論（一句）

**IN-11 乾淨咗**：`map 引用 19 / ssot status.* 19`，兩條清單都空。
**IN-21 第一次真跑，同 sync 時嘅 baseline 逐字對得上**：17 對 pair、`TOTAL DEAD 0`、
16 對 `missing 0` ＋ 1 對已知 `missing 7`。
**三條重量過嘅 baseline（IN-06 2947 / IN-08 353 / IN-12 243）全部重現。**

## Decision table

| ID | one line | first seen | my read | founder |
|----|----------|-----------|---------|---------|
| — | 今次 run **冇** Outstanding mismatch | — | — | — |
| IN-11 | 上次（`f92e7de`）報嘅 mismatch，今次 **match** | 2026-08-10 | 已修，符合 `eb19c41` 嘅 expected | ☐ 確認可結案 |
| IN-21 | 首跑，17 pair `TOTAL DEAD 0`；1 對 `missing 7` 係 case 講明嘅 baseline | 2026-08-11 | case 明寫 `missing` 只守「唔准升」，**唔係**結論 | ☐ 嗰 7 條要唔要補 |
| （scope 簿記） | 檔頭 Lane budget 寫 `verified 12 · unverified 6`，實數 **11 / 7** | 2026-08-11 | 加 IN-21 嗰陣只加咗 `static 10→11`，冇加 unverified 一格 | ☐ 下次 sync 執 |

## Outstanding

**冇。** 今次 run 冇任何 case 出現 expected 同 actual 唔一致。

## 上次 mismatch 嘅 close-out — [IN-11]

- **上次（2026-08-10，`f92e7de`）actual**：三條清單都唔空 —— 8 個 `OrderStatus` 冇 key、
  4 個 map key 唔喺 enum（`PENDING_PAYMENT` / `AWAITING_MEETUP` /
  `MEETUP_AUTHENTICATING` / `RECEIVED_BY_AUTHENTICATOR`）。
- **今次（`eb19c41`）actual**：

  ```
  map 引用 19   ssot status.* 19
  map 有、ssot 冇（會印 key 名俾用戶）: []
  ssot 有、map 冇引用（孤兒 key）    : []
  ```

- **對 expected**：case 寫「兩條清單一世都要空、兩個數要相等」。**達標。**
- **覆蓋限制（老實講低）**：呢條係 `static` lane，佢證明嘅係 **key ↔ ssot 一對一**。
  IN-11 保護嗰個 status chip **只喺 order conversation 先 render**；呢個 UAT DB 現成嗰單
  對話（listing `cmsc1iz6l000oy4lvpo9zkqh3`，賣家 Tom，由 `/listing/<id>` 個「聯絡賣家」入）
  係 **listing enquiry**，chip 行緊 `listingInquiry` 分支。**冇為咗追個 chip 去開 order**
  （task 明示唔好，我亦冇造任何 order）。所以：**「chip 真係 render 出英文/中文 label
  而唔係 raw enum 名」呢件事，今次冇喺畫面上驗過** —— 嗰個係 IN-17（`browser`，spec 未寫）。
- **scope last synced**：`eb19c41`（**0 commits behind HEAD**）。

## Unverified（7 條，唔計入總數）

| ID | lane | 狀況 |
|---|---|---|
| **IN-21** | `static` | **行咗，逐行對得上 baseline。** 17 對 pair、`TOTAL DEAD 0`、冇 `MAP NOT FOUND`。`missing` 唯一非零 = `apps/authenticator/app/authenticate/[orderId]/page.tsx:STATUS_LABEL` **missing 7**（`AUTH_RECEIVED_PENDING_SELLER_ACK` `AWAITING_BUYER_PICKUP` `CUSTODY` `DELIVERED_PENDING_AUTH_ACK` `HANDOVER_TO_AUTH` `PAYMENT_EXPIRED` `SELLER_ACK_PENDING`），其餘 16 對 `missing 0` —— 同 sync 時記錄嘅**一模一樣**。條 case 有實測輸出，下次 `/qa sync i18n` 可以升 `verified`。 |
| IN-15 | `curl` | **行咗，兩條 assertion 都對。** `from=//evil.com/x` → `location: http://localhost:3018/`（打返自己 origin 根，冇去 evil.com）；`lang=fr` → `set-cookie: lang=zh`。另外實測三個 portal 個 `route.ts` **`diff` 完全一樣**（consumer==authenticator==admin），所以打 consumer 一個就夠呢個前提今次成立。可升 `verified`。 |
| IN-16 | `curl` | **行咗，符合「已知限制」嘅寫法。** `curl -H 'Cookie: lang=en' localhost:3018/browse \| grep -c '[一-鿿]'` → **1**（> 0，SSR 仍然出中文，同 backlog §3.6 一致）。可升 `verified`。 |
| IN-03 | `browser` | **行唔到 —— spec 未寫。** |
| IN-17 | `browser` | **行唔到 —— spec 未寫**，authenticator 半邊另外被帳號擋住（見下）。 |
| IN-18 | `browser` | **行唔到 —— spec 未寫。** |
| IN-20 | `browser` | **行唔到 —— spec 未寫**（同 IN-17 共用嗰個 spec）。 |

### Browser lane —— 我真係行咗個 runner，唔係靠估

冇用 grep source 代答任何一條 browser case（README 明令唔准）。實際證據：

```
$ ./docs/qa/browser/run.sh conversation
▸ pulling mcr.microsoft.com/playwright:v1.49.0-jammy (removed again when the run finishes)
▸ running browser lane (grep: conversation)
Error: No tests found
▸ removing image
```

`docs/qa/browser/tests/` 得三個 spec：`browse-mobile.spec.ts` · `seller-profile.spec.ts` ·
`share-modal.spec.ts`（＋`fixtures.ts`）。`grep -ril "lang=en\|conversation\|language"`
喺成個 tests 目錄**零命中**。即係 IN-03 / IN-17 / IN-18 / IN-20 四條**冇任何 spec 可以行**。

**呢四條係本次 run 最大嘅覆蓋窿**，而且正正係 `eb19c41` 改嗰嚿嘢嘅 runtime 對照：
static 證明咗 key 齊、map 齊，但**冇任何嘢證明個 chip 喺畫面上 render 成點**。

### Authenticator portal 帳號封鎖（影響 IN-17 半邊）—— 同上次一樣

```
POST localhost:4010/api/auth/login
  qa-buyer@demo.hk     -> 201
  qa-seller@demo.hk    -> 201
  qa-auth@authentik.hk -> 401   ← 未 seed 過
```

即係 IN-17 就算 spec 寫好，**authenticator 嗰半邊仍然係 blocked（唔係 fail）**。
要行得到需要：(a) 喺 `authentik_uat` seed 一個 QA 專用鑑定師帳號並開通，或者
(b) founder 明示批准借 `milan@authentik.hk`（scope 檔寫「暫時借 milan」，runbook 寫
「QA 唔准掂 demo 帳號」—— 兩句仍然打交，我唔會自己揀）。

## Skipped

| ID | Why |
|---|---|
| — | 冇。計數嘅 11 條全部行到，冇任何 `curl`/`static` case 被環境擋住。 |

## Manual（2 條，交返 founder release checklist）

| ID | 一句 | 點解要人眼 |
|---|---|---|
| IN-01 | Footer 語言 select 見到、撳得到；「樣衰唔起眼」係刻意嘅 | 冇機器門檻；有人「順手執靚咗」反而係偏離 ruling |
| IN-19 | `lang=en` 之後 top-nav / browse chip / checkout 掣 / sell label / ConversationPane header 排版唔爆 | 英文長過中文 50–100%；`eb19c41` 新加嗰 8 條英文 status label（`In authenticator custody` / `Awaiting seller drop-off` 等）**全部長過原本嘅中文**，個 chip 係窄嘢，值得順手望一眼 |

## Matched（11 條計數 + 3 條 unverified 額外行）

| ID | one line | 實際輸出 |
|---|---|---|
| IN-05 | `packages/utils` 個 `build` 冇夾 `compile-locales`（守門員） | `{'compile-locales': 'npx tsx ../../scripts/compile-locales.ts', 'build': 'tsc -p tsconfig.build.json', 'type-check': 'tsc --noEmit'}` —— `build` 冇夾，達標 |
| IN-06 | `data.ts` 同 `ssot.json` 完全同步（**重量過嘅 2947**） | `ssot 2947 data.ts 2947 missing 0 stale 0 extra 0`；`namespaces 67`。**新 baseline 重現** |
| IN-07 | 英文字串冇中文標點／懸空標點／雙空格 | `en 帶中文標點: 0   懸空標點/雙空格: 0`（硬零達標；`eb19c41` 新加嗰 8 條英文冇污染） |
| IN-08 | 重複 zh／en drift 冇再開多（**重量過嘅 353 / 167**） | `同 zh 重複: 353   其中 en 唔一致: 167` —— 兩個數**啱啱好等於** baseline，冇升 |
| IN-09 | orphan namespace 清單同 baseline 一致 | `39 orphan namespaces / 1206 keys`。逐個對過 = `admin.*` 13 + `authenticator.{onboarding,profile,earnings,branches,messages,authenticate}` 6 + `auth.dashboard` + `privacy`/`terms`/`notFound` + `utils.*` 10 + `ui.{chip,confirmDialog,handoverHistory,listingThumb,otpInput,payoutDisclaimer}` 6 = 39，**冇新面孔** |
| IN-10 | 冇 `t()` 叫緊 ssot 冇嘅 key | `檢查咗 1232 個字面 key（另有 33 個由變數傳入，查唔到）` + `PASS`，exit **0**。1232 同 baseline 一樣（冇無故跌） |
| **IN-11** | pane status key ↔ ssot 嚴格一對一 | `map 引用 19   ssot status.* 19`，兩條清單**都空**。**上次嘅 mismatch 已清** |
| IN-12 | consumer page-scan 合計同逐版數字全中（**重量過嘅 243**） | 合計 **243**；`messages 73` · `privacy 60` · `terms 59` · `sell 29` · `orders 19` · `about 1` · `account/profile 1` · `s/[id] 1`，**16 個 ✓ 全部維持 0**。逐格對得上 |
| IN-13 | `packages/ui` 未 wire 嘅 component 冇長出新硬編中文 | `total 35`；`handover-history 16 · confirm-dialog 6 · language-switcher 4 · tier-pill 3 · otp-input 2 · payout-disclaimer 2 · chip 1 · listing-thumb 1`。**`skip (RS-09 owns): packages/ui/src/components/conversation-pane.tsx` 有印出嚟** —— 即係 `createT` 冇被人拆走，IN-13/RS-09 條界仍然守得住 |
| IN-14 | 硬 code `'zh-HK'` 冇升 | `apps/consumer 9 · apps/authenticator 2 · apps/admin 20 · packages/ui 1`，四個數全等於 baseline |
| IN-02 | 三個 portal 語言 route 都 307 + set cookie + 跳返 `from` | 三個都 `307`，`location` 分別指返 `:3018/browse`、`:3011/browse`、`:3013/browse`；`set-cookie: lang=en; Path=/; Expires=Tue, 10 Aug 2027 17:58:33 GMT; Max-Age=31536000; SameSite=lax` |
| （額外）IN-21 | 17 對 enum↔map pair | `TOTAL DEAD 0`，16 對 `missing 0` ＋ `authenticate/[orderId]:STATUS_LABEL missing 7`。冇 `MAP NOT FOUND` |
| （額外）IN-15 | `from=` 唔做到 open redirect、`lang=` 唔認得 fall back `zh` | `location: http://localhost:3018/`；`set-cookie: lang=zh` |
| （額外）IN-16 | SSR 一律出中文（已知限制） | `grep -c '[一-鿿]'` → **1**（> 0，符合條 case 刻意寫成現況嘅 expected） |

### 附註 A — IN-02 個 `Expires=` 差異（同上次一樣，仍然唔入 Outstanding）

同 case 記低嘅 2026-08-03 baseline 唯一分別仍然係**多咗個 `Expires=` attribute**，
同 `Max-Age=31536000` 一致，三個 portal 一致。條 case 個 assertion 本身冇講「唔准有其他
attribute」，所以**唔當 mismatch**。想收緊係改 case 嘅事。
另外「三個 portal 都照收 `from=/browse` 然後跳自己 domain 嘅 `/browse`」今次**原樣重現**
（`:3011` / `:3013` 都跳咗個唔存在嘅 `/browse`）—— scope 講明未有 ruling、只記錄。

### 附註 B — scope 檔嘅簿記數（唔係 product，唔算 mismatch）

`docs/qa/scope/frontend/i18n.md` 檔頭「Lane budget」寫：
`Status：verified 12（命令實測過）· unverified 6 · manual 2`。
實際逐條數：**verified 11**（IN-02 / 05 / 06 / 07 / 08 / 09 / 10 / 11 / 12 / 13 / 14）·
**unverified 7**（IN-03 / 15 / 16 / 17 / 18 / 20 / **21**）· manual 2 = 20。
加 IN-21 嗰陣只更新咗 `static 10 → 11`，冇順手 +1 落 unverified。
**呢個係 scope 檔自己嘅數，唔係件產品**，所以唔入 Outstanding —— 交返下次 `/qa sync i18n` 執。
（今次報告個 Lane budget 一行用嘅係**實數 7**，唔係檔頭嗰個 6。）

## Test data left on UAT

**冇造任何持久測試數據。** 全部 read-only：

- static case 淨係**讀** working tree 入面嘅檔 —— 冇寫過任何 tracked 檔，
  冇行過 `compile-locales.ts`（IN-06 明令唔准用「行一次再 diff」嘅做法寫花 `data.ts`）。
- curl case 全部 GET（`/browse` ×1、`/api/locale` ×5、`/api/listings` ×1、四個 root ping）。
- 唯一 write-ish 動作 = **3 次 `POST /api/auth/login`**（`qa-buyer` 201 / `qa-seller` 201 /
  `qa-auth` 401），冇攞 token 去做任何嘢。
- **冇開 listing、冇落 order、冇 payment、冇 share preview、冇 payout intent、冇上載任何檔、
  冇掂任何 R2 bucket、冇 DB write。** 所以今次冇 `qa-20260811-b` 前綴嘅嘢留低。
- `npm install` 冇行過；冇開過任何 dev server；founder 三個 process 冇掂過。
