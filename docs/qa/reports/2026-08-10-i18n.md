# QA — i18n — 2026-08-10

Run id: `qa-20260810-a`

**Deployment verified:** UAT 呢次唔係 Docker stack，係 founder 喺呢部機行緊嘅 local dev
server，直接食住呢個 git working tree（API `:4010` / consumer `:3018` /
authenticator `:3011`；admin `:3013` 都行緊，順手用埋）。所以「container 行緊舊 code」
呢個陷阱結構上唔存在 —— 證據：`git rev-parse --short HEAD` = **`f92e7de`**（正正係
scope 個 `last_synced_commit`），`git status --porcelain` 淨係兩個 **docs/qa scope 檔**
有改動，**冇任何 product code 未 commit**。三個 port 全部應機（3018/3011/3013 → 200，
4010 `/api/listings` 回真 payload）。
**Substitution 記錄**：scope 入面所有打 `uat.certifinehk.com` / `uat-auth` / `uat-admin`
嘅命令，一律改打 `localhost:3018` / `:3011` / `:3013`，`uat-api` 改打 `localhost:4010`。

Lane budget: curl 3 · static 10 · browser 4 · manual 2（唔跑）· unverified 6（唔計數）
Cases: **11 run · 10 matched · 1 mismatched · 0 skipped**
Known, unchanged: 0（known.md 五條全部唔屬呢個 scope）

> 計數口徑：19 條之中，6 條標 `unverified`（IN-03 / IN-15 / IN-16 / IN-17 / IN-18 /
> IN-20）按 README 規矩唔計入總數，2 條 `manual`（IN-01 / IN-19）唔屬我。實際跑咗
> 嘅 11 條 = static 10 + curl 1。另外我**順手行埋** IN-15 / IN-16 兩條 curl（見下面
> 「Unverified」一節），結果照記低但**唔計入 11 條入面**。

## Decision table

| ID | one line | first seen | my read | founder |
|----|----------|-----------|---------|---------|
| IN-11 | `STATUS_KEY` 覆蓋唔到 `OrderStatus` 18 個值入面 8 個；另有 4 個 map key 唔喺 enum | **new**（sync 時已見到，今次正式打） | 睇唔出係邊邊錯 —— 要 founder 判 | ☐ |

## Outstanding

### MISMATCH [IN-11] `static` `observed` — 由變數傳入嘅 status key 冇覆蓋晒 `OrderStatus` enum

- **expected**：三條清單全部空 —— 即係每個 `OrderStatus` 都有對應
  `ui.conversation.status.<X>`，而個 map 冇任何唔存在嘅狀態名。
- **actual**：三條都唔空。18 個 enum 值入面 **8 個冇 key**，另有 **4 個 map key 唔喺 enum**。
- **evidence**（原封不動行 scope 入面條 script）：

  ```
  enum 18
  enum 冇 key : ['AWAITING_PAYMENT', 'HANDOVER_TO_AUTH', 'SELLER_ACK_PENDING', 'CUSTODY',
                 'AUTH_RECEIVED_PENDING_SELLER_ACK', 'AWAITING_BUYER_PICKUP',
                 'DELIVERED_PENDING_AUTH_ACK', 'PAYMENT_EXPIRED']
  map 有但 enum 冇: ['listingInquiry', 'PENDING_PAYMENT', 'MEETUP_AUTHENTICATING',
                     'RECEIVED_BY_AUTHENTICATOR', 'AWAITING_MEETUP']
  ssot 冇       : ['AWAITING_PAYMENT', 'HANDOVER_TO_AUTH', 'SELLER_ACK_PENDING', 'CUSTODY',
                   'AUTH_RECEIVED_PENDING_SELLER_ACK', 'AWAITING_BUYER_PICKUP',
                   'DELIVERED_PENDING_AUTH_ACK', 'PAYMENT_EXPIRED']
  ```

  `listingInquiry` 係另一個 family（`conversationType === 'listing'` 嗰個 chip，
  `conversation-pane.tsx:841`），照 case 註明**唔算**，所以實質係 **4** 個。

  Fallback 行為喺 `packages/ui/src/components/conversation-pane.tsx:837`：

  ```tsx
  {STATUS_KEY[orderStatus] ? _t(STATUS_KEY[orderStatus]!) : orderStatus}
  ```

  即係 map 撈唔到就**原樣印個 enum 名**落 chip（唔會炸、唔會 fallback 去中文）。
  `STATUS_KEY` 全份喺 `:226–241`。

- **同 sync 時嘅觀察一致**：sync（2026-08-10，`f92e7de`）記錄嘅係同一批名，
  今次正式跑出嚟一個字都冇變 —— 即係唔係 flaky，係穩定嘅現況。
- **交叉參照**：`AWAITING_PAYMENT` 呢個名喺同一個檔 `:851` 嘅 `PAIR_BLOCKED`
  set 入面**有用到**（`new Set(['AWAITING_PAYMENT','REFUNDED','DISPUTED','COMPLETED'])`），
  即係 pane 自己都預咗會收到呢個狀態；但 `STATUS_KEY` 就淨係識 `PENDING_PAYMENT`。
  呢點對 founder 判斷應該有用，但**我唔會用佢落判斷** —— 兩個 set 各有各嘅用途。
- **scope last synced**：`f92e7de`（**0 commits behind HEAD** —— 條 case 係全新嘅，
  唔存在「舊咗」呢個解釋，所以差異一定係真差異，只係唔知差喺邊邊）。

**二選一（founder 揀）**：
`STATUS_KEY` / `ui.conversation.status.*` **應該補齊 18 個 `OrderStatus`**（即係個 map
追唔上 state machine，係真 bug，chip 有機會印 raw enum 名俾用戶睇），
**定係** 嗰 8 個狀態（custody / handover / ack / payment-expired 嗰批）**根本唔會**
喺一個對話 pane 出現，而 `PENDING_PAYMENT` / `AWAITING_MEETUP` /
`MEETUP_AUTHENTICATING` / `RECEIVED_BY_AUTHENTICATOR` 係 API 層另一套 display status
（咁 IN-11 就要收窄成「只驗會出現喺對話嘅子集」，順便寫低嗰個子集係邊幾個）？

## Skipped

| ID | Why |
|---|---|
| — | 冇。counted 嘅 11 條全部行到。 |

## Unverified（6 條，唔計入總數）

| ID | lane | 狀況 |
|---|---|---|
| IN-15 | `curl` | **順手行咗，兩條 assertion 都對得上。** `from=//evil.com/x` → `location: http://localhost:3018/`（打返自己 origin 根）；`lang=fr` → `set-cookie: lang=zh`。條 case 而家有實測輸出，下次 `/qa sync i18n` 可以升做 `verified`。 |
| IN-16 | `curl` | **順手行咗，符合「已知限制」嘅寫法。** `curl -H 'Cookie: lang=en' localhost:3018/browse \| grep -c '[一-鿿]'` → **1**（> 0，SSR 仍然出中文）。同樣可以升做 `verified`。 |
| IN-03 | `browser` | **行唔到 —— spec 未寫。** `docs/qa/browser/tests/` 得 `browse-mobile` / `seller-profile` / `share-modal` 三個 spec，冇任何 locale spec。 |
| IN-17 | `browser` | **行唔到 —— spec 未寫**（`./docs/qa/browser/run.sh conversation` 冇嘢 match）。**而且 authenticator 半邊另外被帳號擋住**，見下。 |
| IN-18 | `browser` | **行唔到 —— spec 未寫**（`run.sh language` 冇嘢 match）。 |
| IN-20 | `browser` | **行唔到 —— spec 未寫**（同 IN-17 共用嗰個 spec）。 |

### Authenticator portal 嘅帳號封鎖（影響 IN-17 半邊）

`qa-auth@authentik.hk` 喺呢個 UAT DB **未 seed 過**，runbook 有寫但實際登入唔到：

```
POST localhost:4010/api/auth/login
  qa-buyer@demo.hk   -> 201
  qa-seller@demo.hk  -> 201
  qa-auth@authentik.hk -> 401
```

即係話 IN-17 就算 spec 寫好咗，**authenticator 嗰半邊仍然係 blocked（唔係 fail）**。
要行得到，需要其中一樣：(a) 喺 `authentik_uat` seed 一個 QA 專用鑑定師帳號並開通，
或者 (b) founder 明示批准今次借 `milan@authentik.hk`（scope 檔自己都寫住「暫時借 milan」，
但 runbook 又寫住 QA 唔准掂 demo 帳號 —— 呢兩句而家係打交嘅，founder 拍板前我唔會揀）。
Consumer 嗰半邊冇呢個問題：`qa-buyer` 登入得，listing `cmsc1iz6l000oy4lvpo9zkqh3`
有現成對話做入口。

## Manual（2 條，交返 founder release checklist）

| ID | 一句 | 點解要人眼 |
|---|---|---|
| IN-01 | Footer 語言 select 見到、撳得到；「樣衰唔起眼」係刻意嘅 | 冇機器門檻；有人「順手執靚咗」反而係偏離 ruling |
| IN-19 | `lang=en` 之後 top-nav / browse chip / checkout 掣 / sell label / ConversationPane header 排版唔爆 | 英文長過中文 50–100%，`c3dcfb5` 一次過 wire 咗 14 版，每版都係第一次有人用英文睇 |

## Matched

| ID | one line | 實際輸出 |
|---|---|---|
| IN-05 | `packages/utils` 個 `build` 冇夾 `compile-locales`（守門員，唔准順手優化） | `{'compile-locales': 'npx tsx ../../scripts/compile-locales.ts', 'build': 'tsc -p tsconfig.build.json', 'type-check': 'tsc --noEmit'}` |
| IN-06 | `data.ts` 同 `ssot.json` 完全同步 | `ssot 2943 data.ts 2943 missing 0 stale 0 extra 0` |
| IN-07 | 英文字串冇中文標點、冇懸空標點／雙空格 | `en 帶中文標點: 0   懸空標點/雙空格: 0`（硬零，達標） |
| IN-08 | 重複 zh／en drift 冇再開多 | `同 zh 重複: 352   其中 en 唔一致: 167` —— 同 baseline 一模一樣，冇升 |
| IN-09 | orphan namespace 清單同 baseline 一致 | `39 orphan namespaces / 1206 keys`；逐個對過 = admin 13 + authenticator 6 + `auth.dashboard` + `privacy`/`terms`/`notFound` + `utils.*` 10 + `ui.*` 6，**冇新面孔** |
| IN-10 | 冇 `t()` 叫緊 ssot 冇嘅 key | `檢查咗 1232 個字面 key（另有 33 個由變數傳入，查唔到）` + `PASS`，exit 0 |
| IN-12 | consumer page-scan 合計同逐版數字全中 | 合計 **236**；66/60/59/29/19/1/1/1 完全對得上，**16 個 ✓ 全部維持 0** |
| IN-13 | `packages/ui` 冇長出新硬編中文 | `total 36`；`handover-history 16 · confirm-dialog 6 · language-switcher 4 · tier-pill 3 · otp-input 2 · payout-disclaimer 2 · chip 1 · listing-thumb 1 · **conversation-pane 1**`。逐行核過 conversation-pane 剩低嗰 1 行正正係 `:342` `// 初次載入訊息 shimmer`（行尾註解，唔係 UI 字串），同 case 寫嘅一致 |
| IN-14 | 硬 code `'zh-HK'` 冇升 | `apps/consumer 9 · apps/authenticator 2 · apps/admin 20 · packages/ui 1`，四個數全等於 baseline |
| IN-02 | 三個 portal 語言 route 都 307 + set cookie + 跳返 `from` | 見下面「附註」 |

### 附註 — IN-02 有一個**冇進 Outstanding** 嘅細微差異（請 founder 過目）

三個 portal 全部照 expected 行（307 / `set-cookie: lang=en; Path=/; Max-Age=31536000;
SameSite=lax` / `location` 指返 `/browse`）：

```
localhost:3018 -> 307, location: http://localhost:3018/browse
localhost:3011 -> 307, location: http://localhost:3011/browse
localhost:3013 -> 307, location: http://localhost:3013/browse
set-cookie: lang=en; Path=/; Expires=Tue, 10 Aug 2027 15:52:03 GMT; Max-Age=31536000; SameSite=lax
```

同 case 記低嘅 2026-08-03 baseline 唯一分別：**多咗個 `Expires=` attribute**。
佢同 `Max-Age=31536000` 一致（一年後），係 cookie serializer 正常會補嘅嘢，
而且三個 portal 一致。我**冇**當佢係 mismatch，因為條 case 嘅 assertion 本身冇講
「唔准有其他 attribute」。如果 founder 想收緊，嗰個係改 case 嘅事。
另外 case 註明嗰個「三個 portal 都照收 `from=/browse` 然後跳自己 domain 嘅 `/browse`」
現象**今次原樣重現**（`:3011` / `:3013` 都跳咗個唔存在嘅 `/browse`）——
scope 講明呢個未有 ruling、只記錄，所以照記錄，唔入 Outstanding。

## Test data left on UAT

**冇造任何持久測試數據。** 今次全部係 read-only：static case 淨係讀 working tree
入面嘅檔（冇寫過任何 tracked 檔），curl case 全部 GET。唯一嘅 write-ish 動作係
3 次 `POST /api/auth/login`（`qa-buyer` 201 / `qa-seller` 201 / `qa-auth` 401）——
冇開 listing、冇落 order、冇 share preview、冇 payout intent、冇上載任何嘢。
Founder 嗰三個 dev server process（27556 / 27637 / 27638）由頭到尾冇掂過。
