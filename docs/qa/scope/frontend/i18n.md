---
layer: frontend
feature: i18n
owners:
  - locales/**
  - packages/utils/src/locales/**
  - apps/consumer/app/api/locale/**
  - apps/authenticator/app/api/locale/**
  - apps/admin/app/api/locale/**
  - packages/ui/src/components/language-switcher.tsx
  # ── 2026-08-10 shared sweep 執返嚟嘅（見檔尾「Sweep 記錄」）──
  - packages/ui/src/components/conversation-pane.tsx
  - packages/ui/src/index.ts
  - packages/ui/package.json
  - apps/consumer/components/conversation-pane.tsx
  - apps/authenticator/components/conversation-pane.tsx
  - apps/consumer/components/footer.tsx
  - apps/authenticator/app/layout.tsx
  - apps/admin/app/layout.tsx
  # ── 2026-08-11 sync（eb19c41）補入 ──
  # IN-12 數緊呢兩版；佢哋各自抄咗一份 OrderStatus map，係 IN-21 十七個 pair 之中兩對
  - apps/consumer/app/messages/page.tsx
  - apps/authenticator/app/messages/page.tsx
  # IN-11 / IN-21 兩條 case 直接讀 `enum OrderStatus` / `DeliveryMethod` / `PaymentMethod`
  # / `OfferStatus` / `ListingStatus`。schema 改咗 = 呢兩條嘅 expected 即刻變
  - apps/api/prisma/schema.prisma
  # 呢個 scope 一半 case 就係跑呢三個 script，佢哋改咗 = case 嘅 assertion 會變
  - scripts/compile-locales.ts
  - scripts/i18n-check-keys.ts
  - scripts/i18n-page-scan.ts
  # ── 2026-08-15 sync（e8b5ce1）補入 ──
  # 鑑定師 portal 語言掣搬咗屋（founder 2026-08-12），IN-18 而家要睇呢個檔先過得到
  - apps/authenticator/app/settings/page.tsx
  # order-status.ts 而家係 STATUS_LABEL_BASE 嘅 SSOT（`statusLabel(status, locale?)`），
  # IN-11/IN-21/IN-12 講嘅 order-status 文案由呢度出，之前 owners 完全冇提過呢個檔
  - packages/utils/src/order-status.ts
  # IN-21 PAIRS、IN-12 表本身已經直接讀呢兩個檔，但 owners 冇寫，跟 08-11 sweep 記錄
  # 「非 packages/ 嗰邊有窿」同一個病，執埋佢
  - apps/consumer/app/orders/page.tsx
  - apps/consumer/app/orders/[id]/page.tsx
last_synced_commit: e8b5ce1
# 沿革：2026-08-11 `/qa sync rules` 曾經改過呢個檔嘅 IN-12 / IN-13（founder ruling：
#    RS-09 擴闊，兩條唔准重疊），但嗰次唔算 i18n sync（`f92e7de..21f183f` 喺本 scope
#    owners 入面係空 diff），所以 last_synced_commit 當時冇郁。2026-08-11 sync 由
#    f92e7de 行到 eb19c41，IN-11 個 founder ruling 已落地，baseline 全部重量過。
#    2026-08-15 sync（呢次）由 eb19c41 行到 e8b5ce1：鑑定師 portal 語言掣搬去
#    /settings（IN-18）、consumer /orders IA 重寫 + STATUS_LABEL_BASE 接 SSOT、
#    domain package 拆分。全部 baseline 用本地 checkout 重新跑過（唔係打 UAT ——
#    sync 唔驗部署，淨係驗 scope 檔同 code 對唔對得上）。
---

# i18n — Frontend

> **Note（唔係 case）：未 wire 嘅頁唔係 bug。** 清單見 `docs/backlog/i18n-backlog.md`。
> 呢句以前佔住 [IN-04] 一個 case ID，但佢冇 pass/fail 條件，行唔到亦 skip 唔到，
> 只會令「幾多條 case」失真。2026-08-03 改做檔頂 note，ID 已刪。

> **呢個 scope 答嘅問題：改完呢一嚿嘢之後，件產品仲係咪完整雙語？**
> 分四組：**pipeline 完整性**（SSOT → data.ts）· **key 正確性**（唔會將 key 名印俾用戶睇）·
> **畫面覆蓋**（key 存在唔等於 component 有用）· **runtime**（cookie／render／格式）。

## Baseline 嘅玩法（重要）

呢個 suite 好多條係 **「唔准比 baseline 差」**，唔係「一定要零」。
點解：`locales/ssot.json` 有 2,943 條 key、67 個 namespace，好多歷史遺留（admin 完全未
wire、authenticator 一半未 wire）。要求硬零 = 每次 run 都吐幾百行已知噪音，跟住冇人再睇。
Baseline 全部係 **2026-08-11 sync 時喺 `eb19c41` 實測嘅數字**，寫死喺條 case 入面
（`locales/ssot.json` 而家 2,947 條 key、67 個 namespace）。

Baseline 變好（數字跌）→ **唔係 mismatch**，係有人做咗嘢，順手喺條 case 更新個數。
Baseline 變差（數字升）→ mismatch，而且報告要列出新增嗰幾行係邊個檔。

**「實測 …（sync）」= 條命令行得通、當時輸出係咁**，唔等於行過一次正式 QA run。

### `eb19c41` 之後改咗數嘅 case（2026-08-11）

`eb19c41` 將四個 order-status map 對齊 `enum OrderStatus`，順帶改咗 ssot key 名。
受影響嘅 baseline **全部重量過**，舊數字一個都冇留低：

| case | 舊 | 新 | 點解 |
|---|---|---|---|
| IN-06 | 2943 | **2947** | `ui.conversation` 90 → 94（19 個 status key）|
| IN-08 | 重複 352 | **353** | 新 key `等買家取貨` 撞 `authenticator.inbox.status.*`；drift 仍然 167 |
| IN-11 | 三條清單都唔空 | **兩條都空**（已重寫，見下） | founder ruling：map 落後 state machine，已修 |
| IN-12 | `messages` 66 / 合計 236 | **73 / 243** | 同一個 map 喺 consumer `messages/page.tsx` 由 14 條加到 18 條，嗰版未 wire i18n，字面中文照計 |
| IN-09 · IN-07 · IN-10 · IN-13 · IN-14 | — | **原數不變**（已喺 `eb19c41` 重量） | 見各條 |

## Lane budget

`curl` 3 · `static` 11 · `browser` 4 · `manual` 2 —— 共 20 條。
Status：`verified` 12（命令實測過）· `unverified` 6 · `manual` 2（唔標 status）。
（2026-08-11：加 IN-21，`static` 由 10 變 11。）

---

## A. Pipeline 完整性 —— SSOT 同 generated artifact 有冇甩

- [IN-05] `static` `verified` — 改完 `locales/ssot.json` 要行 `npx tsx scripts/compile-locales.ts` 再 rebuild utils
  - `python3 -c "import json;print(json.load(open('packages/utils/package.json'))['scripts'])"`
  - 實測 2026-08-03：`{'compile-locales': 'npx tsx ../../scripts/compile-locales.ts',
    'build': 'tsc -p tsconfig.build.json', 'type-check': 'tsc --noEmit'}` —— `build` **冇**夾 `compile-locales`
  - ⚠️ `packages/utils/package.json` 個 `build` script **唔可以**加 `compile-locales`
    （`scripts/` 唔喺 Docker image 入面，會炸 `ERR_MODULE_NOT_FOUND`）
  - 理由：呢條係「唔好順手優化」嘅守門員 —— 兩個 script 分開係有原因嘅

- [IN-06] `static` `verified` — `data.ts` 一定要同 `ssot.json` 一模一樣（**key 數、zh 值、en 值全對**）
  - ```bash
    python3 - <<'PY'
    import json,re
    s=json.load(open('locales/ssot.json'))
    src=open('packages/utils/src/locales/data.ts').read()
    zh_blk=src.split('export const zhMap',1)[1].split('export const enMap',1)[0]
    en_blk=src.split('export const enMap',1)[1]
    parse=lambda b: dict(re.findall(r'^  "([^"]+)": `((?:[^`\\]|\\.)*)`',b,re.M))
    Z,E=parse(zh_blk),parse(en_blk)
    unesc=lambda v: v.replace('\\`','`').replace('\\${','${').replace('\\\\','\\')
    keys={f"{n}.{k}" for n,i in s.items() for k in i}
    miss=[f"{n}.{k}" for n,i in s.items() for k in i if f"{n}.{k}" not in Z]
    stale=[f"{n}.{k}" for n,i in s.items() for k,v in i.items()
           if f"{n}.{k}" in Z and (unesc(Z[f"{n}.{k}"])!=v['zh'] or unesc(E.get(f"{n}.{k}",''))!=(v['en'] or v['zh']))]
    extra=[k for k in Z if k not in keys]
    print("ssot",len(keys),"data.ts",len(Z),"missing",len(miss),"stale",len(stale),"extra",len(extra))
    for x in (miss+stale+extra)[:20]: print("  ",x)
    PY
    ```
  - **實測 2026-08-11（sync，`eb19c41`）：`ssot 2947 data.ts 2947 missing 0 stale 0 extra 0`**
    （舊數 2943；`eb19c41` 淨加 4 條 —— `ui.conversation.status.*` 12 舊 → 19 新，
    即 +8 −4 = +4。`namespaces: 67` 冇變）
  - **實測 2026-08-15（sync，`e8b5ce1`）：`ssot 3010 data.ts 3010 missing 0 stale 0 extra 0`，
    `namespaces: 68`（+1）**。2947 → 3010 = +63，主要嚟自 consumer `/orders` IA 重寫
    （四組 tab / role chip / 爭議 banner / 三方 meetup 通知全部進咗 SSOT，`e8b5ce1` commit
    message 講明）＋ order-status.ts 接線帶嚟嘅新 key。新 namespace = `orders.group.*` 呢類
  - Expected：**三個數字全部 0**。任何一個唔係 0 = 有人改咗 `ssot.json` 但冇行
    `compile-locales.ts`（或者行咗但冇 commit `data.ts`）
  - 理由：`data.ts` 係 **commit 咗嘅 generated artifact**，而 compile step 唔喺任何 build
    script 入面（見 IN-05，而且係刻意）。即係話「唔記得行」呢件事**冇任何自動閘攔得住** ——
    ssot 加咗新字，app 照 build、照 deploy，用戶見到 key 名。呢條係嗰個閘
  - ⚠️ 呢條唔准改成「行一次 `compile-locales.ts` 再 `git diff`」—— 嗰個做法會**寫花**
    working tree 入面一個 tracked 檔，QA 唔准改 product code

- [IN-16] `curl` `unverified` — **SSR 一律出中文（已知限制，唔係 mismatch）**
  - `curl -s -H 'Cookie: lang=en' https://uat.certifinehk.com/browse | grep -c '[一-鿿]'`
  - Expected：**> 0（即係 server-rendered HTML 仍然係中文）**，英文喺 hydrate 之後先出
  - 理由：`getClientLocale()` 喺 `useEffect` 入面行 —— 見 `docs/backlog/i18n-backlog.md` §3.6。
    呢條**故意將 expected 寫成現況**，咁 run 就唔會每次重新「發現」佢一次再食 founder 注意力。
    **佢由 0 變返有值先係要報嘅嘢** —— 咁即係 §3.6 個 middleware fix 落咗地，
    要返轉頭改 backlog 同呢條 case。爬蟲／SEO 收到中文版嘅後果都記喺 §3.6

---

## B. Key 正確性 —— 唔好將 key 名印俾用戶睇

- [IN-10] `static` `verified` — 冇任何 `t()` 叫一個 `ssot.json` 冇嘅 key
  - `npx tsx scripts/i18n-check-keys.ts`
  - **實測 2026-08-11（sync，`eb19c41`）：`檢查咗 1232 個字面 key（另有 33 個由變數傳入，查唔到）` → PASS，exit 0**
    （同 `f92e7de` 一樣。`eb19c41` 改嘅全部係變數傳入嗰 33 個入面，所以呢條數紋風不動 ——
    正正就係 IN-11 / IN-21 存在嘅理由）
  - **實測 2026-08-15（sync，`e8b5ce1`）：`檢查咗 1267 個字面 key（另有 36 個由變數傳入，查唔到）` → PASS，exit 0**
    （1232→1267 = orders IA 重寫新增嘅字面 `t()` call；33→36 變數傳入嗰批，同 STATUS_LABEL_BASE
    由 record 改做 function 有關 —— 呢兩個數只准升，冇違反 expected）
  - Expected：**exit 0 + `PASS`**；literal key 數只可以升唔可以無故跌
  - 理由：`t()` 查唔到就原樣返回個 key 名。TypeScript 睇唔到、build 唔會炸、
    畫面直接出 `orders.status.authenticating` 俾用戶睇。2026-08-10 orders 頁就係咁，
    靠人手對 key 先捉到

- [IN-11] `static` `verified` — **pane 由變數傳入嘅 status key 同 ssot 嚴格一對一**
  - ```bash
    python3 - <<'PY'
    import re,json
    pane=open('packages/ui/src/components/conversation-pane.tsx').read()
    mapped=set(re.findall(r"'(ui\.conversation\.status\.\w+)'",pane))
    ss=json.load(open('locales/ssot.json'))['ui.conversation']
    ssot_status={f"ui.conversation.{k}" for k in ss if k.startswith('status.')}
    print("map 引用",len(mapped),"  ssot status.*",len(ssot_status))
    print("map 有、ssot 冇（會印 key 名俾用戶）:",sorted(mapped-ssot_status))
    print("ssot 有、map 冇引用（孤兒 key）    :",sorted(ssot_status-mapped))
    PY
    ```
  - **實測 2026-08-11（sync，`eb19c41`）：`map 引用 19   ssot status.* 19`，兩條清單都空。**
  - Expected：**兩條清單一世都要係空，`19 / 19` 兩個數要相等**（數字本身可以升跌 ——
    schema 加一個狀態就會變 20/20 —— 但**唔准分叉**）
  - **⚠️ 呢條嘅 expected 講緊「必須維持空」，唔係「現時係咁」。** 佢喺 `f92e7de` 嗰陣
    實測係**唔空**（8 個 enum 值冇 key、4 個 map 名唔喺 enum），2026-08-10 報做 mismatch；
    founder 2026-08-11 判：**map 落後 state machine，係真 bug**（理據：七個 caller 全部
    原封不動傳 `Order.status`，入到 pane 嘅值一定係 `OrderStatus` member）。
    `eb19c41` 修好，baseline 由「唔空」變「空」。舊嗰個非空清單**唔再係 expected**，
    再見到非空 = 新嘅倒退，照報
  - 理由：`i18n-check-keys.ts` **明講**佢查唔到由變數傳入嘅 key（33 個），呢條就係補嗰個窿嘅
    **i18n 半邊** —— key 名砌得出但 ssot 冇，`t()` 原樣吐個 key 名俾用戶睇
  - **同 IN-21 嘅界線**：呢條只睇 **key ↔ ssot**（翻譯有冇齊）。
    **map ↔ prisma enum**（狀態有冇齊）已經由 **IN-21** 全 repo 一齊守，包埋呢個 pane。
    兩條唔重疊：IN-11 唔再讀 `schema.prisma`，IN-21 唔讀 `ssot.json`

- [IN-21] `static` `unverified` — **所有由 Prisma enum 做 key 嘅 label/color map，唔准有死 entry**
  - ```bash
    python3 - <<'PY'
    import re
    sch=re.sub(r'//.*','',open('apps/api/prisma/schema.prisma').read())
    E={m.group(1):[x for x in m.group(2).split() if re.fullmatch(r'[A-Z][A-Z_0-9]*',x)]
       for m in re.finditer(r'enum (\w+) \{([^}]*)\}',sch)}
    PAIRS=[
     ('packages/ui/src/components/conversation-pane.tsx','STATUS_KEY','OrderStatus'),
     ('packages/ui/src/components/conversation-pane.tsx','STATUS_COLOR','OrderStatus'),
     ('packages/ui/src/components/conversation-pane.tsx','OFFER_STATUS_KEY','OfferStatus'),
     ('apps/consumer/app/messages/page.tsx','STATUS_LABEL','OrderStatus'),
     ('apps/consumer/app/messages/page.tsx','DELIVERY_LABEL','DeliveryMethod'),
     ('apps/consumer/app/messages/page.tsx','DELIVERY_LABEL_NEUTRAL','DeliveryMethod'),
     ('apps/consumer/app/messages/page.tsx','PAYMENT_METHOD_LABEL','PaymentMethod'),
     ('apps/authenticator/app/messages/page.tsx','STATUS_LABEL','OrderStatus'),
     ('apps/authenticator/app/messages/page.tsx','DELIVERY_LABEL','DeliveryMethod'),
     ('apps/consumer/app/orders/page.tsx','DELIVERY_LABEL_KEY','DeliveryMethod'),
     ('apps/consumer/app/orders/page.tsx','PAYMENT_LABEL_KEY','PaymentMethod'),
     ('apps/consumer/app/orders/[id]/page.tsx','DELIVERY_LABEL_KEY','DeliveryMethod'),
     ('apps/consumer/app/orders/[id]/page.tsx','PAYMENT_LABEL_KEY','PaymentMethod'),
     ('apps/consumer/app/my-listings/page.tsx','DELIVERY_LABEL_KEY','DeliveryMethod'),
     ('apps/consumer/app/listing/[id]/page.tsx','STATUS_LABEL_KEY','ListingStatus'),
     ('apps/authenticator/app/authenticate/[orderId]/page.tsx','STATUS_LABEL','OrderStatus'),
     ('apps/authenticator/app/authenticate/[orderId]/page.tsx','DELIVERY_LABEL','DeliveryMethod'),
    ]
    dead=0
    for path,name,en in PAIRS:
        src=open(path).read()
        m=re.search(r'const '+name+r'\s*:\s*Record<string,\s*string>\s*=\s*\{(.*?)\n\};',src,re.S)
        if not m: print("MAP NOT FOUND",path,name); dead+=99; continue
        keys={k for k in re.findall(r'(?:^|[{,\s])([A-Z][A-Z_0-9]*)\s*:',m.group(1))}
        d=sorted(keys-set(E[en])); miss=sorted(set(E[en])-keys)
        dead+=len(d)
        print(f"{name:24} {path}")
        print(f"   vs {en}: dead={d or '[]'}  missing={len(miss)} {miss if miss else ''}")
    print("TOTAL DEAD",dead)
    PY
    ```
  - **實測 2026-08-11（sync，`eb19c41`）：17 對 pair、`TOTAL DEAD 0`。**
    `missing` 只有一處非零：`apps/authenticator/app/authenticate/[orderId]/page.tsx:STATUS_LABEL`
    **missing 7**（`AUTH_RECEIVED_PENDING_SELLER_ACK` `AWAITING_BUYER_PICKUP` `CUSTODY`
    `DELIVERED_PENDING_AUTH_ACK` `HANDOVER_TO_AUTH` `PAYMENT_EXPIRED` `SELLER_ACK_PENDING`）；
    其餘 16 對 `missing 0`
  - Expected：
    - **`TOTAL DEAD 0` 係硬零**。死 entry = 個名喺 schema 唔存在，永遠 match 唔到，
      呢個方向冇任何合法解釋
    - **`MAP NOT FOUND` 一出現就要處理** —— map 改咗名／搬咗屋／改咗 type annotation，
      條 case 靜咗聲就等於冇守。呢個係 pair list 手寫嘅代價，寫明咗
    - `missing` 嗰邊**只守「唔准升」**，baseline = 上面嗰 16×0 + 1×7。
      **唔係要求硬零**：部分 map 係刻意只收幾個狀態（`authenticate/[orderId]` 係鑑定師工作台，
      根本睇唔到付款／取貨階段）。**嗰 7 條要唔要補，係 founder 嘅 call，唔係呢條 case 嘅結論**
  - ⚠️ **`PAIRS` 係人手維護嘅**，同 RS-11 嗰張 admin import 清單一樣。
    新增一個 enum-keyed map 就要加一行。點搵：
    `grep -rn "Record<string, string>" apps/consumer apps/authenticator apps/admin packages/ui --include='*.tsx' | grep -v node_modules`
    然後**自己揀**邊啲真係由 enum 落 key。2026-08-11 掃過，全部 `Record<string, string>`
    之中確認咗呢 17 個；以下三個**刻意唔收**（睇落似，其實唔係 enum-keyed）：
    | map | 點解唔收 |
    |---|---|
    | `apps/authenticator/app/page.tsx:VERDICT_PILL_CLS` | 收 `AUTH_INCONCLUSIVE`，唔喺 `OrderStatus`；佢跟嘅係 `authVerdict`（`inbox/page.tsx:255` 見到 `'INCONCLUSIVE'`），另一個 domain |
    | `apps/admin/app/authenticators/page.tsx:APP_STATUS_LABEL` | 鑑定師**申請**狀態（`SUBMITTED`／`NEEDS_MORE_INFO`／…），唔係 `AuthenticatorStatus`（`PENDING`／`ACTIVE`／`SUSPENDED`／`REMOVED`）|
    | `apps/admin/app/analytics/page.tsx:OUTCOME_COLORS` | analytics 自己砌嘅 bucket，`IN_FLIGHT` 係合成值 |
    收咗佢哋就會即刻吐 5 條假陽性（實測過）
  - 理由：IN-11 捉到嘅唔係一個 typo，係**一整類 defect** —— `Record<string, string>`
    由一個 Prisma enum 落 key，但兩邊冇任何嘢綁住：`Record<string, string>` 個 index type
    係 `string`，TypeScript 對打錯／過時嘅 key **一聲都唔出**，build 過、deploy 到、
    畫面直接印個 raw enum 名。`eb19c41` 一次過喺**四個** map 搵到同一個病
    （pane 兩個 + 兩版 messages 各一個，仲要 messages 嗰兩個係獨立抄咗一份、連
    `AUTH_IN_PROGRESS` 呢個 schema 叫 `AUTHENTICATING` 嘅名都有），
    而其中三個 map 從來冇任何 case 睇住。呢條就係將「靠 IN-11 撞啱一個檔」變成
    「17 對 pair 每次都查」
  - **status `unverified`**：條命令喺 sync 時行通咗、上面個 baseline 係真嘅輸出，
    但未經過一次正式 QA run，所以未計入報告總數（README「Status」節）

- [IN-09] `static` `verified` — 冇 key 寫咗但**冇任何 component 用**（orphan namespace）
  - ```bash
    python3 - <<'PY'
    import json,re,subprocess
    s=json.load(open('locales/ssot.json'))
    out=subprocess.run(['bash','-lc',
      "grep -rho \"_\\?t(\\s*'[a-zA-Z][A-Za-z0-9_.]*'\" apps packages --include='*.tsx' --include='*.ts' 2>/dev/null | grep -v node_modules"],
      capture_output=True,text=True).stdout
    used=set(re.findall(r"'([A-Za-z][\w.]*)'",out))
    orph=[ns for ns in s if not any(u.startswith(ns+'.') for u in used)]
    print(len(orph),"orphan namespaces /",sum(len(s[n]) for n in orph),"keys")
    for n in orph: print("  ",n,len(s[n]))
    PY
    ```
  - **Baseline（2026-08-11 sync，`eb19c41` 重量）：39 個 namespace / 1,206 條 key —— 同 `f92e7de` 一樣。**
    `eb19c41` 改嘅全部喺 `ui.conversation` 入面，而 `ui.conversation` 本身唔係孤兒
    （pane 一直用緊），所以 rename ＋ 淨加 4 條 key **一條都冇跨越孤兒邊界**。
    逐個 namespace 對過，清單同下面 39 個逐字相同
    合理嘅 39 個：`admin.*` 全部 13 個（admin 完全未 wire，backlog §4）·
    `authenticator.{onboarding,profile,earnings,branches,messages,authenticate}` +
    `auth.dashboard`（backlog §2）· `privacy` `terms` `notFound`（backlog §3）·
    `utils.*` 10 個同 `ui.{chip,confirmDialog,handoverHistory,listingThumb,otpInput,payoutDisclaimer}`
    —— 呢啲係「key 寫咗，component 未收」
  - **Baseline（2026-08-15 sync，`e8b5ce1` 重量）：36 個 namespace / 1,166 條 key（跌咗，做咗嘢，
    唔係 mismatch）。** 跌走嘅 3 個：`ui.payoutDisclaimer`（`payout-disclaimer.tsx` 而家有
    `createT`，實測 IN-13）、`tier-pill` 同 `offer-card` 對應嘅 namespace 已經 wire（`offer-card`
    本身唔係呢 39 個之一，但 skip 行印得出證實佢有 `createT`）。清單其餘 33 個同 08-11 一樣。
    `authenticator.messages` 仍然係孤兒（48 條）—— 鑑定師 portal `messages/page.tsx` 呢次
    改咗 171 行（DELIVERED_LABEL / STATUS_LABEL 對齊 enum），但**冇**接 i18n，
    同 IN-12 表入面 authenticator 未進度一致
  - Expected：**orphan 清單同上面 39 個一模一樣**。
    - 有新 namespace 加入清單 = 有人加咗 key 但冇 wire component（或者 wire 咗又拆走）
    - 有 namespace 離開清單 = 做咗嘢，**更新 baseline，唔係 mismatch**
  - ⚠️ 呢條有**假陽性**：全部 key 都用變數砌出嚟嘅 namespace 會被當成 orphan。
    見到新嘢入清單，要開個檔望一眼先當數
  - 理由：`b7e6aaa` 合併 ConversationPane 之後，`authenticator.messages`（48 條 key）
    即刻變咗孤兒 —— 冇人用、冇人刪、下次有人改文案就會改錯嗰份。
    呢條就係捉「refactor 之後 SSOT 冇跟住收」

---

## C. 字典本身嘅衞生 —— 標點同重複

- [IN-07] `static` `verified` — 英文字串唔准帶中文標點，亦唔准有懸空標點／雙空格
  - ```bash
    python3 - <<'PY'
    import json,re
    s=json.load(open('locales/ssot.json'))
    P='。、，「」『』：；！？（）〈〉《》'
    cjk=[(f"{n}.{k}",v['en']) for n,i in s.items() for k,v in i.items() if any(c in P for c in v['en'])]
    dang=[(f"{n}.{k}",v['en']) for n,i in s.items() for k,v in i.items()
          if re.search(r'\s[.,:;!?]\s*$|\(\s*\)|\s{2,}',v['en'])]
    print("en 帶中文標點:",len(cjk),"  懸空標點/雙空格:",len(dang))
    for x in (cjk+dang)[:20]: print("  ",x)
    PY
    ```
  - **實測 2026-08-11（sync，`eb19c41`）：`en 帶中文標點: 0   懸空標點/雙空格: 0`**
    （`eb19c41` 新加嗰 8 條英文 —— `Pending payment` / `In authenticator custody` /
    `Awaiting seller drop-off` 等 —— 全部乾淨，硬零守得住）
  - Expected：**兩個都 0**（硬零 —— 呢條係少數幾條真係做到零嘅）
  - ⚠️ `·` `…` `–` `≥` **唔算**中文標點，全形清單刻意冇包佢哋：
    `Tier 1 · < $1,000`、`Loading…` 係正常英文排版。條清單擴闊咗就會即刻吐 146 條假陽性
  - 理由：`。` `、` `：` **唔喺 `[一-鿿]` 範圍**，所以任何靠漢字 grep 嘅檢查（包括 RS-09
    原本嗰版、包括 `i18n-page-scan.ts` 個 `HAN`）都睇唔到佢哋。2026-08-10 codemod 剝走
    漢字但留低句尾粒 `。`，英文版變咗 `Privacy Policy .`，一路 ship 到出街

- [IN-08] `static` `verified` — 同一句中文寫喺兩個 namespace，然後只改咗一邊（drift）
  - ```bash
    python3 - <<'PY'
    import json,collections
    s=json.load(open('locales/ssot.json'))
    by=collections.defaultdict(list)
    for n,i in s.items():
        for k,v in i.items(): by[v['zh']].append((f"{n}.{k}",v['en']))
    dup=[r for r in by.values() if len(r)>1]
    drift=[r for r in dup if len({e for _,e in r})>1]
    print("同 zh 重複:",len(dup),"  其中 en 唔一致:",len(drift))
    for r in drift[:10]: print("  ",r)
    PY
    ```
  - **Baseline（2026-08-11 sync，`eb19c41`）：重複 **353** 組，其中 en 唔一致 **167** 組。**
  - **352 → 353 係 `eb19c41` 帶嚟，已核實逐組**（同 `f92e7de` 逐 group diff 過）：
    | 組 | 變化 |
    |---|---|
    | `等買家取貨` | **新一組** — `ui.conversation.status.AWAITING_BUYER_PICKUP` 撞 `authenticator.inbox.status.AWAITING_BUYER_PICKUP`（＋1）|
    | `待付款` | 成員換名，`…status.PENDING_PAYMENT` → `…status.AWAITING_PAYMENT`（組數不變）|
    | `鑑定師簽收` | 成員換名，`…RECEIVED_BY_AUTHENTICATOR` → `…AUTH_RECEIVED_PENDING_SELLER_ACK`（組數不變）|
    新嗰組**兩邊 en 一致**（都係 `Awaiting buyer pickup`），所以 drift 冇升，仍然 167。
    呢個係「新狀態一定要有 key，而個 key 唔可以跨 namespace 借」嘅必然結果，
    **唔係有人求其抄多份**
  - Expected：**兩個數都唔准升過 353 / 167**。升咗就列出新增嗰幾組（同一句中文、兩個唔同英文）
  - **實測 2026-08-15（sync，`e8b5ce1`）：重複 356 組（+3），其中 en 唔一致 169 組（+2）——
    已核實逐組，baseline 更新：**
    | zh | 新增組員 | 點解 |
    |---|---|---|
    | `待鑑定` | `authenticator.nav.inbox`（`To authenticate`）／`authenticator.dashboard.stat.pending.label`（`Pending Authentication`）| 兩個 en 唔一致 → drift |
    | `QR 交收` | `authenticator.nav.scan`（`QR handover`）／`authenticator.scan.page.title`（`QR Handover`）| 大小寫唔一致 → drift |
    | `收入` `設定` | `authenticator.nav.*` 對 `authenticator.{earnings,settings}.page.title` | en 一致，純重複，冇加 drift |
    | `待處理`（原有 dup 組加新成員）| 新成員 `orders.group.action` = `Needs you`，同組原有 `Pending` | orders IA 重寫（`9c7d157`／`b856919`）攞咗個現成中文詞
    「待處理」做四大分組之一，但語意其實係「需要你郁手」，同錢包／admin 嗰個「等緊處理」唔同 → **新 drift** |
    ⚠️ 最後一組（`待處理`）**唔係 namespace 撞名嗰種抄漏**，係兩個真係唔同嘅概念揀咗同一句中文，
    founder 若果想改文案避開呢個 drift，係 enhancement，唔係呢條 case 要擋嘅嘢 —— 規矩只守
    「唔准再加新嘅」，呢 3 組已經加咗，寫實落 baseline
  - ⚠️ **唔准當佢係「應該係零」** —— 好多重複係有理由嘅（`載入中…` 四個 namespace 各有一份，
    `全部品類` browse 同 homepage 各一份）。呢條守嘅係「唔好再開多啲」，
    唔係要求今日合併晒。要合併係 enhancement，founder 拍板先
  - 理由：`messages.unreadCount`（`{n} 個未讀`）同 `authenticator.messages.unreadCount`
    （`· {count} 個未讀`）就係咁 drift 出嚟 —— 連 placeholder 名都唔同咗。
    中文改咗一邊、英文改咗另一邊，兩個 portal 之後永遠對唔返

---

## D. 畫面覆蓋 —— key 存在唔等於 component 有用

- [IN-12] `static` `verified` — consumer 每個 page（連埋佢 import 嘅嘢）剩低幾多硬編中文
  - `npx tsx scripts/i18n-page-scan.ts --all apps/consumer`
  - **Baseline（2026-08-11 sync，`eb19c41`，合計 243）：**

    | page | 剩 |
    |---|---:|
    | `messages/page.tsx` | 73 ⬆ |
    | `privacy/page.tsx` | 60 |
    | `terms/page.tsx` | 59 |
    | `sell/page.tsx` | 29 |
    | `orders/page.tsx` | 19 |
    | `about/page.tsx` | 1 |
    | `account/profile/page.tsx` | 1 |
    | `s/[id]/page.tsx` | 1 |
    | 其餘 **16** 個 page | **0 ✓** |

    嗰 16 個零：`account/wallet` `account/wallet/methods` `account/wallet/payouts`
    `auth/complete-profile` `auth/link-confirm` `authenticator/[id]` `browse`
    `buyer/[id]` `checkout/[orderId]` `listing/[id]` `login` `my-listings`
    `orders/[id]` `page.tsx`（首頁）`register` `seller/[id]`
  - **Baseline（2026-08-15 sync，`e8b5ce1`，合計 234，跌咗 9）：**

    | page | 剩 | 變化 |
    |---|---:|---|
    | `messages/page.tsx` | 72 | −1 |
    | `privacy/page.tsx` | 60 | 不變 |
    | `terms/page.tsx` | 59 | 不變 |
    | `sell/page.tsx` | 29 | 不變 |
    | `orders/page.tsx` | **11** | **−8** |
    | `about/page.tsx` | 1 | 不變 |
    | `account/profile/page.tsx` | 1 | 不變 |
    | `s/[id]/page.tsx` | 1 | 不變 |
    | 其餘 16 個 page | 0 ✓ | 不變 |

    `orders/page.tsx` 19→11 係 `9c7d157`＋`e8b5ce1` 嘅結果：STATUS_LABEL_BASE 由 hardcode
    中文 record 改做 `statusLabel(status, locale?)`（`packages/utils/src/order-status.ts`），
    連埋四組 tab 文案、role chip、爭議 banner 一齊入咗 SSOT。**呢個係跌咗，做咗嘢，
    唔係倒退** —— 更新 baseline，冇 mismatch 要報。`messages/page.tsx` 73→72 屬雜項改動，
    唔喺呢次 commit 嘅主力範圍
  - ⚠️ **owners 補漏**：`orders/page.tsx` / `orders/[id]/page.tsx` 一直冇喺呢個 scope 嘅
    `owners` 出現，即使呢條 case 同 IN-21 都直接讀佢哋 —— 同 08-11 sweep 記錄嗰個「非
    `packages/` 嗰邊有窿」一樣嘅病。已經喺呢次 sync 補入 `owners`（見檔頭）
  - **`messages/page.tsx` 66 → 73（合計 236 → 243）係 `eb19c41` 帶嚟，已核實**：
    嗰版自己抄咗一份 `STATUS_LABEL`，由 14 條加到 18 條對齊 `enum OrderStatus`
    （淨加 `HANDOVER_TO_AUTH` `SELLER_ACK_PENDING` `CUSTODY`
    `AUTH_RECEIVED_PENDING_SELLER_ACK` `AWAITING_BUYER_PICKUP`
    `DELIVERED_PENDING_AUTH_ACK` `PAYMENT_EXPIRED`，減 `AUTH_IN_PROGRESS` 換名），
    每條 label 都係字面中文。**呢版未 wire i18n**（`grep -c createT` = 0），
    所以字面中文喺呢度唔算違反 —— 佢淨係推高 IN-12 個數。
    **Baseline 升咗要記返點解，唔可以當佢冇發生**：呢 7 行係 IN-21 收窄 map 嘅代價，
    等 `messages/page.tsx` wire 埋 i18n 就會一次過清返落去
  - Expected：**嗰 16 個要一直維持 0**；其餘 8 個唔准升過上表。零變非零 = 有人喺一版已經收乾淨嘅
    畫面加返中文（呢個係最痛嗰種，因為佢唔會炸）
  - ✓ **同 RS-09 唔會重覆報**：consumer `messages/page.tsx` 同 authenticator 嗰版
    `grep -c createT` 都係 **0**，即係兩版都唔喺 RS-09 嗰 46 個檔入面（2026-08-11 實測）。
    上面新增嗰 7 行淨係 IN-12 數
  - ⚠️ 呢個 script **唔會跟住 package import 入去**（`@authentik/ui` 唔喺佢範圍，
    佢自己個 header 講明）。所以 `messages/page.tsx` 嗰 66 條**唔包括** ConversationPane。
    `packages/ui` 由兩條分管：**已 wire** 嗰批（即 ConversationPane）歸 **RS-09**，
    **未 wire** 嗰 8 個歸 **IN-13**（2026-08-11 re-cut）
  - 理由：`c3dcfb5` 一次過 wire 咗 14 版。呢張表就係嗰次嘅成果證明 ——
    冇呢個 baseline，倒退返去冇人知。同時記住 `fe5abf4` 個教訓：
    「page 做完」唔等於「畫面做完」，所以一定要行 `--all`（跟 import graph），唔好淨係 grep page 檔

- [IN-13] `static` `verified` — **`packages/ui` 入面「未 wire」嘅共用 component 唔准長出新嘅硬編中文**
  - ```bash
    python3 - <<'PY'
    import re,os
    HAN=re.compile(r'[一-鿿。、「」！？：；]')
    strip=lambda s: re.sub(r'^\s*//.*$','',re.sub(r'/\*[\s\S]*?\*/','',re.sub(r'\{/\*[\s\S]*?\*/\}','',s)),flags=re.M)
    tot=0
    for root,_,fs in os.walk('packages/ui/src'):
        for f in sorted(fs):
            if f.endswith('.tsx'):
                p=os.path.join(root,f); src=open(p).read()
                if 'createT' in src:          # ← 已 wire = RS-09 嘅地盤，唔喺呢度數
                    print("  skip (RS-09 owns):",p); continue
                n=len([l for l in strip(src).split('\n') if HAN.search(l)])
                if n: print(f"{n:4}  {p}"); tot+=n
    print("total",tot)
    PY
    ```
  - **Baseline（2026-08-11 sync `eb19c41` 重量，合計 35 行 / 8 個檔 —— 同 re-cut 時一樣）：** `handover-history` 16 ·
    `confirm-dialog` 6 · `language-switcher` 4 · `tier-pill` 3 · `otp-input` 2 ·
    `payout-disclaimer` 2 · `chip` 1 · `listing-thumb` 1
    ＋ script 會印一行 `skip (RS-09 owns): packages/ui/src/components/conversation-pane.tsx`
  - **Baseline（2026-08-15 sync，`e8b5ce1` 重量，合計 33 行 / 7 個檔 —— 跌咗，做咗嘢，唔係
    mismatch）：** `handover-history` 16 · `confirm-dialog` 6 · `language-switcher` 4 ·
    `otp-input` 2 · `chip` 1 · `listing-thumb` 1 · **新出現** `version-badge` 3
    （`packages/ui/src/components/version-badge.tsx`，`671a3dc` 加嘅 build-stamp 元件，
    未 wire）。`tier-pill`（3）同 `payout-disclaimer`（2）跌走 —— 兩個而家都有 `createT`，
    script 印多咗兩行 `skip (RS-09 owns): …tier-pill.tsx` / `…payout-disclaimer.tsx`。
    `offer-card.tsx` 呢次由 apps 層搬入 `packages/ui`，但佢一開始就帶 `createT`，
    一直都係 skip，唔生新行
  - Expected：**八個檔嘅數一個都唔准升，total 維持 35**；`conversation-pane` 一定要出現喺
    skip 嗰行（佢冇咗 = 有人拆走咗 `createT`，兩條 case 就會同時漏咗佢）
  - `eb19c41` 郁咗 `conversation-pane.tsx`（＋46 行，內含中文註解）但 **total 冇變** ——
    因為佢仍然有 `createT`，照樣 skip。實測 skip 行仍然印得出，界線守得住
  - **舊 baseline 36（2026-08-10）減 1 = 35**：嗰 1 行係 `conversation-pane.tsx:342` 行尾註解
    `// 初次載入訊息 shimmer`，而家歸 RS-09 數。**唔係有人執走咗嘢**
  - ⚠️ `language-switcher` 嗰 4 行係 `繁` / `繁體中文` / `切換至繁體中文` —— **語言名本身唔應該翻譯**，
    唔准「順手收入 ssot」
  - **同 RS-09 嘅界線（founder ruling 2026-08-11，`/qa sync rules` 執行）**：
    RS-09（`docs/qa/scope/static/rules.md`）條 grep 已經擴闊到
    `grep -rl createT apps/*/app apps/*/components packages/*/src/components`，
    即係話 `packages/ui` 唔再係佢範圍外。兩條 case 而家**用 wiring 狀態切**，唔係用路徑切：

    | | 收邊啲檔 | 現時 |
    |---|---|---|
    | **RS-09** | 全 repo 入面**有** `createT` 嘅（app + shared） | 46 個檔，含 `conversation-pane.tsx` |
    | **IN-13** | `packages/ui/src` 入面**冇** `createT` 嘅 | 8 個檔 |

    兩邊嘅 predicate 係嚴格互補（`'createT' in src` vs `not in`），
    **同一行 source 唔可能兩邊都報**。IN-13 條 script 加咗 `continue` 就係執行呢條界。
  - **點解唔索性 retire IN-13**（founder 明確俾咗呢個選項）：因為佢守緊一樣 RS-09 **按定義**
    唔守嘅嘢。RS-09 嘅語義係「**已經做完嘅唔准倒退**」，未 wire 就唔算違反 ——
    咁 `handover-history`（16 行）呢啲未 wire 嘅共用 component 就會完全冇人睇住，
    再長多 20 行硬編中文都冇聲出。IN-13 補嗰個窿：未 wire 可以，但**唔准再長大**。
    所以佢係收窄，唔係退休

- [IN-14] `static` `verified` — 硬 code 咗 `'zh-HK'` 嘅日期／數字格式（唔係 key，翻唔到）
  - `for r in apps/consumer apps/authenticator apps/admin packages/ui; do echo -n "$r: "; grep -roP "toLocale\w*\('zh-HK'" $r --include='*.tsx' | wc -l; done`
  - **Baseline（2026-08-11 sync，`eb19c41` 重量）：** `apps/consumer 9` · `apps/authenticator 2` ·
    `apps/admin 20` · `packages/ui 1`（`handover-history.tsx:27`）—— 四個數同 `f92e7de` 一樣
  - **實測 2026-08-15（sync，`e8b5ce1`）：** `apps/consumer 9`（不變）· `apps/authenticator 1`
    （**−1**，跌咗）· `apps/admin 20`（不變，未 wire 唔理）· `packages/ui 1`（不變）。
    Authenticator 少咗一個 `toLocale…('zh-HK')`，同 `messages/page.tsx` 呢次改動有關；
    跌咗係做咗嘢，baseline 更新，唔係 mismatch
  - Expected：**consumer / authenticator / packages/ui 三個數唔准升**（admin 未 wire，唔理）
  - ⚠️ `toLocaleString('en-HK')` **唔算違反** —— 銀碼格式全站一律 `en-HK`（`HK$12,345`），
    唔跟語言行。只有**日期／時間**先要跟 locale
  - 正確寫法喺 `packages/ui/src/components/conversation-pane.tsx:39` `intlTag(locale)`：
    由 `TLocale` 揀 `en-HK` / `zh-HK`，唔好寫死
  - 理由：呢類嘢**冇 key、冇漢字**，所以 page-scan、check-keys、RS-09 三個都捉唔到 ——
    英文版照樣出「2026年8月10日」。`f92e7de` 喺 pane 入面執走咗幾個，但冇任何嘢阻止佢喺第二度重新長返出嚟

---

## E. Runtime —— cookie、render、切換

- [IN-02] `curl` `verified` — 切換語言 → `/api/locale?lang=X&from=…` set cookie 再 full reload
  - `curl -s -o /dev/null -D - "https://uat.certifinehk.com/api/locale?lang=en&from=/browse"`
  - 實測 2026-08-03，三個 portal（`uat` / `uat-auth` / `uat-admin`）全部：
    **307** + `Set-Cookie: lang=en; Path=/; Max-Age=31536000; SameSite=lax` + `Location:` 指返 `/browse`
  - ⚠️ 三個 portal 都收 `from=/browse` 然後照跳，即使 `/browse` 只喺 consumer 存在
    （authenticator / admin 會跳去自己 domain 嘅 `/browse`）。呢個未有 ruling，只記錄。
  - 理由：cookie + full reload 係定咗嘅機制（唔行 client-side i18n context），
    改成 SPA 切換會令 server-rendered meta 對唔上語言

- [IN-15] `curl` `unverified` — `from=` 唔可以做到 open redirect，`lang=` 唔認得就當 `zh`
  - ```bash
    curl -s -o /dev/null -D - "https://uat.certifinehk.com/api/locale?lang=en&from=//evil.com/x"
    curl -s -o /dev/null -D - "https://uat.certifinehk.com/api/locale?lang=fr&from=/browse"
    ```
  - Expected：第一條 **`Location: https://uat.certifinehk.com/`（打回自己個 origin 根，唔准去 evil.com）**；
    第二條 **`Set-Cookie: lang=zh`**（唔認得嘅語言 fall back 去 default，唔係照收）
  - 三個 portal 份 `route.ts` **逐個 byte 一樣**（2026-08-10 `diff` 過），所以呢條打 consumer 就夠
  - 理由：呢個 route 收 user-controlled `from` 再 302 出去。`route.ts` 入面
    `if (redirectUrl.origin !== origin)` 嗰段就係為咗擋呢樣，但從來未有 case 驗過佢真係擋到

- [IN-03] `browser` `unverified` — 已經收乾淨嗰啲 consumer 畫面，set `lang=en` 之後真係零中文
  - 未有 Playwright spec（`docs/qa/browser/tests/` 得 browse-mobile / seller-profile / share-modal）。
    要寫：set cookie `lang=en` → 逐版 goto → **等 hydrate 完**（見下面 ⚠️）→ 讀 `body.innerText`
    → assert 冇 `[一-鿿。、「」]`
  - 範圍 = IN-12 嗰 16 個零分 page，加 top-nav / footer 兩個共用框架
  - **⚠️ 一定要等 hydrate**：SSR 一律出中文（IN-16 / backlog §3.6），
    load 完即刻讀會**一定**見到中文，report 出嚟係假 mismatch。
    等法：`await expect(page.getByRole('…')).toHaveText(<英文>)` 或者 `waitForFunction`
    等到 `document.body.innerText` 唔再含漢字為止（timeout 5s）
  - 理由：呢條係 IN-12 嘅 runtime 對照。IN-12 證明 source 冇中文字面值，
    但條件 render、API 回嚟嘅字、SSOT 缺 key fallback 返中文 —— 呢三樣淨係睇 source 睇唔到。
    **唔准用 grep bundle 答呢條**：bundle 入面有個英文字串唔等於畫面 render 到佢

- [IN-17] `browser` `unverified` — ConversationPane 喺**兩個 portal** 都轉英文
  - `./docs/qa/browser/run.sh conversation`（spec 待寫）
  - Consumer `uat.certifinehk.com/messages`（`qa-buyer@demo.hk`）同
    authenticator `uat-auth.certifinehk.com/messages`（暫時借 `milan@authentik.hk`，
    未有 QA 專用鑑定師帳號）：set `lang=en` → 打開一單對話 →
    assert 睇得見嘅 chrome（輸入框 placeholder、presence、date divider、offer 卡）**全英文**
  - 理由：`b7e6aaa` 之後兩個 portal 共用同一份 body，但係經**兩個唔同 adapter**
    （`apps/consumer/components/conversation-pane.tsx` / `apps/authenticator/…`）注入
    theme + deps。「consumer 見到英文」**唔證明** authenticator 都得 ——
    合併本身就係為咗令兩邊唔會再分叉，呢條就係量度佢有冇做到

- [IN-18] `browser` `unverified` — 三個 portal 都撳得到語言掣，撳完真係轉
  - `./docs/qa/browser/run.sh language`（spec 待寫；現有 spec `language-switcher.spec.ts` 仲打緊
    authenticator `/login`，要改，見下面 ⚠️）
  - Consumer：footer `variant="select"`（`apps/consumer/components/footer.tsx:37`）→ 揀 `English`
    → assert cookie `lang=en` + 頁面 reload 後 top-nav 出英文。
    **Authenticator：`/settings`**（登入之後，`app/settings/page.tsx:41` `variant="select"`）——
    撳個 select、揀 `English` → assert cookie `lang=en` + reload 後頁面出英文。
    Admin（`app/layout.tsx:17`）：仍然係登入前浮喺右上角嘅 `button` variant pill，位置未郁。
  - **⚠️ 2026-08-15 sync：authenticator 呢半條由 `/login` 搬去 `/settings`，唔係 mismatch**。
    Founder ruling 2026-08-12（`apps/authenticator/app/layout.tsx:17-20` 有明文 comment）：
    「浮喺右上角嘅語言 pill 搬入 `/settings` —— 同 consumer portal 之前搬過嘅 call 一樣。
    成日用嘅 portal，偏好應該喺 nav 入面一個固定位，唔係浮喺內容上面嘅 chip」。
    `2026-08-14-full.md` 報過 [IN-18] 呢個「authenticator `/login` 搵唔到語言 pill」，
    已經核實係 stale case（唔係 regression）——**呢次 sync 就係執返嗰個 stale**。
    登入前而家**冇**語言入口（要登入先切到 `/settings`）；呢個係搬位嘅副作用，
    唔係呢條 case 守嘅嘢，**founder 若果想 pre-login 都揀到語言，係新 ruling，唔係呢條倒退**
  - `b1e9462` 順手修埋一個相關 bug：`button` variant 個 `href` 舊時喺 **SSR** 就計死
    （`window` 唔存在，`currentPath()` fallback 去 `/`），淨係 client mount effect 先會
    喺 cookie 係 `en` 嗰刻修正 —— zh 頁（default）撳落去會擲用戶去 `/` 唔係留喺原頁。
    2026-08-12 喺 authenticator portal 度發現。`onClick` 而家 `preventDefault()` 再喺
    click 嗰刻重新計 `href`，同 `select` variant（一直喺 `onChange` 度計，冇呢個病）睇齊
  - 理由：`b7e6aaa` 將個掣由 `bg-white` 改做 `bg-transparent border-current/20`，
    即係話佢**由「自己帶背景」變成「借主場色」**。admin 深色底之前見到嘅白色團就係咁嚟。
    呢條唔係驗靚唔靚（嗰個係 IN-19），係驗**撳唔撳得到、撳完 cookie 有冇變**

- [IN-20] `browser` `unverified` — 日期／時間格式跟語言行
  - 開 conversation（同 IN-17 個 spec），`lang=en` 之後 date divider 同 message timestamp
    要係 **en-HK 格式（`August 10, 2026` / `Yesterday`）**，唔係 `2026年8月10日` / `昨日`
  - 理由：IN-14 喺 source 層守住「唔好寫死 `zh-HK`」，但 `Intl` 真係點 render 一定要睇畫面。
    而且 `formatDateDivider` 有三條分支（今日／琴日／同週／再舊），
    grep 睇唔出邊條行緊 —— 呢個正正係 `static` 唔准講畫面嗰條界

---

## F. 人眼先判到嘅嘢

- [IN-01] `manual` — Footer 個語言 select 見到；樣衰唔起眼係**刻意**嘅
  - 要人眼判斷「有幾唔起眼」，冇機器門檻
  - 理由：founder 押後咗完整 enhancement（backlog §1），所以呢條唔係要求靚，
    係要求「仲喺度、仲撳得」。有人「順手執靚咗」反而係偏離 ruling

- [IN-19] `manual` — 英文版排版唔爆
  - 掃 `lang=en` 之後嘅 consumer top-nav / browse filter chip / checkout 掣 /
    sell 表單 label / ConversationPane 個 header：英文字普遍長過中文 50–100%，
    睇下有冇 wrap 到兩行、切字、掣撐爆格
  - 理由：機器答唔到「爆咗」。`c3dcfb5` 一次過 wire 咗 14 版，
    每一版都係第一次有人用英文睇 —— 呢類嘢一定要人眼行一次先入 release checklist

---

## 已刪嘅 ID（永不重用）

- **IN-04** — 2026-08-03 改做檔頂 note（見上）。

---

## Sweep 記錄（2026-08-10，`3b8dfcd..f92e7de`）

`git diff 3b8dfcd..HEAD -- packages/` 有 5 個檔：

| 檔 | 關唔關事 | 處置 |
|---|---|---|
| `packages/ui/src/components/conversation-pane.tsx` | **關** — 新增 1,386 行，90 條 `ui.conversation.*` key，自己 `createT` + `getClientLocale` | 入 `owners`；生 IN-11 / IN-13 / IN-17 / IN-20 |
| `packages/ui/package.json` | **關** — `packages/ui` 第一次 depend `@authentik/utils`，i18n 由 app 層落咗 package 層 | 入 `owners` |
| `packages/ui/src/index.ts` | **關** — export 個 shared pane，決定咗兩個 portal 食同一份字典 | 入 `owners` |
| `packages/ui/src/components/language-switcher.tsx` | **關** — 已經喺 `owners`；`bg-white` → `bg-transparent` | 生 IN-18 |
| `packages/utils/src/locales/data.ts` | **關** — 已經喺 `owners`（`packages/utils/src/locales/**`）；+1,330 行 | 生 IN-06 |

Sweep 之前 `owners` **完全冇提** `packages/ui/src/components/conversation-pane.tsx`，
但佢已經係全 repo 單一最大嘅翻譯載體。已補。

---

## Sweep 記錄（2026-08-11，`f92e7de..eb19c41`）

`git diff f92e7de..HEAD -- packages/` 有 **2** 個檔，兩個都已經喺 `owners`（快線行得通，
今次 sweep 冇補到新嘢 —— 咁樣講先係老實，唔係冇做）：

| 檔 | 關唔關事 | 處置 |
|---|---|---|
| `packages/ui/src/components/conversation-pane.tsx` | **關** — `STATUS_KEY` / `STATUS_COLOR` 重寫成 18 個 enum member | 重量 IN-11（改寫）· IN-13（不變）· 生 IN-21 |
| `packages/utils/src/locales/data.ts` | **關** — `ui.conversation.status.*` +8 −4，總數 2943 → 2947 | 重量 IN-06 · IN-08 · IN-09 |

**Owners 反而喺非 `packages/` 嗰邊有窿**（sweep 只掃 `packages/`，呢個係佢掃唔到嘅）：
`eb19c41` 同時改咗 `apps/{consumer,authenticator}/app/messages/page.tsx`，
而 **IN-12 一直喺度數緊 consumer 嗰版**（66 → 73），偏偏兩個檔都唔喺 `owners`。
即係話呢次 sync 如果淨係睇 `owners` diff，IN-12 個 baseline 會靜靜哋錯咗。
已補入 `owners`，連埋 `apps/api/prisma/schema.prisma`（IN-11 / IN-21 直接讀佢個 enum）。

---

## Sweep 記錄（2026-08-15，`eb19c41..e8b5ce1`）

觸發：`/qa sync authenticator-portal`（founder 拍板 IN-18 由 `/login` 搬去 `/settings`，
順手要求檢查 2026-08-14 之後嘅幾項改動有冇拖冧呢個檔）。

`git diff eb19c41..HEAD -- <owners>` 有 10 個檔（見檔頭 `last_synced_commit` 註解），
`git diff eb19c41..HEAD -- packages/` 額外有 25 個檔（`packages/domain` 全新 package +
`packages/api-client` 由 hand-written 改做 codegen + `packages/ui` 幾個新 component）：

| 檔 | 關唔關事 | 處置 |
|---|---|---|
| `apps/authenticator/app/layout.tsx` | **關** — `LanguageSwitcher` 搬走，語言掣移去 `/settings` | 生 IN-18 改寫 |
| `apps/authenticator/app/messages/page.tsx` | **關** — `STATUS_LABEL` 重寫（+157 行），但**未接** i18n | IN-21 pair 不變、IN-09 authenticator.messages 仍孤兒 |
| `apps/consumer/app/messages/page.tsx` | 半關 — 雜項改動，未涉呢個 scope 嘅硬閘 | IN-12 messages 73→72 |
| `apps/consumer/components/footer.tsx` | 唔關 — footer 改動同語言掣本身無關 | 冇 case 受影響 |
| `locales/ssot.json` / `packages/utils/src/locales/data.ts` | **關** — orders IA 重寫全部進咗 SSOT | IN-06/08/09/10/12 baseline 全部重量 |
| `packages/ui/src/components/conversation-pane.tsx` | 已喺 owners，呢次改動細（+8/−…），冇影響 IN-11/21 數字 | 冇 |
| `packages/ui/src/components/language-switcher.tsx` | **關** — 修咗 SSR href bug（`b1e9462`），冇影響 IN-18 expected，但值得記喺理由 | IN-18 補一段 |
| `packages/ui/src/index.ts` | **關** — export 多咗 `VersionBadge` / `OfferCard` / sidebar hook | IN-13 baseline（`version-badge.tsx` 新增未 wire） |
| `apps/api/prisma/schema.prisma` | **關** — 1 行改動，`enum OrderStatus` member 名冇變 | IN-11/21 數字不變，已實測 |
| `scripts/*.ts` | 冇改 | 冇 |
| **shared sweep（`packages/`）** `packages/domain/**`（全新，tier/categories/order-rules/payout-methods/analytics-events/phone/conditions 由 `packages/utils` 搬過去）| **唔關** i18n——搬嘅係 business rule，唔係翻譯字典。`packages/utils/src/locales/**` 冇搬走 | 記錄，唔加 owners |
| **shared sweep** `packages/utils/src/order-status.ts`（**+177/−…，呢個先係真正相關嗰個**）| **關** — STATUS_LABEL_BASE 由 hardcode record 改做 `statusLabel(status, locale?)`，係 orders/messages 頁文案而家嘅單一來源 | **補入 `owners`**（之前完全冇提過，係一個窿） |
| **shared sweep** `packages/api-client/**`（codegen 契約） | **唔關** — DTO / OpenAPI 契約，唔涉字典 | 記錄，唔加 owners |
| **shared sweep** `packages/ui/src/components/{offer-card,resizable-sidebar,version-badge}.tsx`（新檔）| **半關** — `offer-card.tsx` 一開始就有 `createT`（RS-09 收），`version-badge.tsx` 未 wire（IN-13 收），`resizable-sidebar.tsx` 冇文字內容，唔涉字典 | IN-13 baseline 改咗；冇個別加入呢個 scope 嘅 `owners`（IN-13 script 本身 walk 全個 `packages/ui/src` 目錄，唔靠逐檔列名） |

**非 `packages/` 嗰邊嘅窿（同 08-11 sweep 記錄同一個病）**：`apps/consumer/app/orders/page.tsx`
同 `orders/[id]/page.tsx` 一直喺 IN-12 / IN-21 度用緊，但兩個都唔喺 `owners`——
`orders/page.tsx` 呢次仲係全 repo 改得最勁嗰個檔之一（`9c7d157` +295 行、`e8b5ce1` 再改），
如果淨係睇 `owners` diff 就會完全睇漏。已補入 `owners`。

**執行紀律**：全部改咗嘅數字（IN-06/08/09/10/12/13/14）呢次都喺本地 checkout 重新跑晒
上面嗰段內嵌 python／`npx tsx`，唔係憑 commit message 估。`static`／`curl` 類 case 嘅「實測」
喺 sync 語境下淨係指「條命令喺呢個 commit 行得通、輸出係咁」，唔等於一次正式 QA run
（README 已經講明呢個分別）。IN-18 因為係 `browser` case，冇本地瀏覽器環境跑唔到 spec，
淨係改咗 case 同 spec 嘅目標，**下次 `/qa run i18n` 或 `/qa run authenticator-portal`
先會真係執行過**。
