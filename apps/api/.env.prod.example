DATABASE_URL="postgresql://authentik:authentik_dev@localhost:5432/authentik?schema=public"
JWT_SECRET="dev-only-change-in-prod-use-32+-chars-random"
JWT_EXPIRES_IN="7d"
API_PORT=4000
CORS_ORIGIN="http://localhost:3008,http://localhost:3001,http://localhost:3003"

# Consumer URL — where Google SSO callback redirects users (login / link-confirm / complete-profile)
NEXT_PUBLIC_CONSUMER_URL="http://localhost:3008"

# Google SSO (Founder ruling 2026-06-11 — P0 Google only).
# Setup: see docs/google-sso-setup.md
# Leave empty to disable Google button — login page still works with email/password.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:4000/api/auth/google/callback"
